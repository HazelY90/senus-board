import DocumentsView from "@/features/dashboard/components/DocumentsView";

/** Renders the dedicated source-document page without a category sidebar. */
export default function DocumentsPage() {
  return <DocumentsView />;
}
